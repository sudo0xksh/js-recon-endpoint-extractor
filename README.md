# js-recon-endpoint-extractor
A Node.js reconnaissance tool that extracts JavaScript files from a target webpage, searches them for exposed API endpoints, and saves the findings to a report file.  Intended for frontend recon, API discovery, and security learning on authorized targets.
